<div class="content_show">
	<h1>This is the top page</h1>

	<h4>the list of storys↓</h4>
	<% @storys.each do |story| %>
		<p>
			<%= link_to story.title, story_path(story.id) %>
			<%= link_to "edit!", edit_story_path(story.id) %>
		</p>
	<% end %>
</div>

	<%= link_to "Create new Story!", new_story_path %>
</div>
